{
  "name": "mylist-firestore-rules-tests",
  "private": true,
  "scripts": {
    "test:rules": "node --test firestore.rules.test.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
